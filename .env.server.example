# Server-side (private) environment variables.
# These keys are NEVER exposed to the browser.
# DO NOT prefix these with NEXT_PUBLIC_.

# --- Firebase Admin ---
# These are used for server-side actions that require admin privileges.
# For Vercel deployment, these should be set in the Vercel project dashboard.
FIREBASE_PROJECT_ID=
FIREBASE_CLIENT_EMAIL=
FIREBASE_PRIVATE_KEY=

# --- Cloudinary Admin ---
# These are used for server-side actions like signing uploads and deleting files.
CLOUDINARY_API_KEY_1=
CLOUDINARY_API_SECRET_1=

# Library Extented (Optional)
CLOUDINARY_API_KEY_2=
CLOUDINARY_API_SECRET_2=
